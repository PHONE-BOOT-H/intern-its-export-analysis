# Architecture

> 시스템 구조와 주요 설계 결정을 기록합니다.
> 새 컴포넌트 추가 또는 구조 변경 시 업데이트하세요.

---

## 전체 구조

```
[프로젝트명]/
├── [디렉토리 1]/    # [역할 설명]
├── [디렉토리 2]/    # [역할 설명]
└── [디렉토리 3]/    # [역할 설명]
```

---

## 주요 컴포넌트

### [컴포넌트 1]
- **역할**: [무엇을 하는가]
- **위치**: `[경로]`
- **의존성**: [무엇에 의존하는가]
- **인터페이스**: [어떻게 다른 컴포넌트와 통신하는가]

### [컴포넌트 2]
- **역할**: 
- **위치**: 
- **의존성**: 
- **인터페이스**: 

---

## 데이터 흐름

```
[사용자 요청] → [컴포넌트 A] → [컴포넌트 B] → [데이터베이스]
                              ↓
                         [응답 반환]
```

---

## 데이터베이스 스키마

### [테이블/컬렉션 이름]
```sql
CREATE TABLE [이름] (
  id         SERIAL PRIMARY KEY,
  [필드]     [타입]    [제약조건],
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 외부 의존성

| 서비스 | 용도 | 문서 |
|--------|------|------|
| [서비스명] | [왜 사용하는가] | [링크] |

---

## 주요 설계 결정

### [결정 1] — [날짜]
- **결정**: [무엇을 결정했는가]
- **이유**: [왜 그 결정을 했는가]
- **대안**: [고려했던 다른 옵션]
- **트레이드오프**: [장단점]

자세한 결정 기록: [DECISION_LOG.md](DECISION_LOG.md)

---

## 환경 설정

### 개발 환경
```bash
[설정 명령어]
```

### 프로덕션 환경
```bash
[설정 명령어]
```

---

## 보안 고려사항
- [인증 방식]
- [인가 방식]
- [데이터 암호화]
- [환경변수 관리]
